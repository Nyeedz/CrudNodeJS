extends ../default

block content

  include ../templates/nav

  .ui.container.grid.stackble
    .row
    .sixteen.wide.column
      h2.ui.header.blue.dividing Entrar
    .two.wide.column
    .eleven.wide.column
      .form-novoproduto
        form.ui.form(action='', method='', autocomplete='off')
          .ui.field.required
            label(for='email') Email
            input(type='email', name='email', placeholder='Email')
          .ui.field.required
            label(for='password') Senha
            input(type='password', name='password', placeholder='Senha')
          button.ui.button.basic.green Iniciar sessão
          .ui.message.error
